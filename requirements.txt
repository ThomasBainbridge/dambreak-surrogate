# Python side of the pipeline: design, metric extraction, surrogates, figures.
# The CFD step itself needs OpenFOAM v2312 (see README). The committed results
# were produced with Ubuntu 22.04's system packages (numpy 1.21, scipy 1.8,
# scikit-learn 0.23, matplotlib 3.5); requirements-lock.txt pins a current,
# tested environment.
numpy>=1.21
scipy>=1.8
scikit-learn>=0.23
matplotlib>=3.5
joblib>=0.17
pillow>=9.0

# --- Development ----------------------------------------------------------
# pytest>=7.0
